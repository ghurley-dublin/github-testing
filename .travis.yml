sudo: false

language: java

env:
    - EMAIL=ghurley@murex.com

script: 
    - mvn clean install -Dmaven.javadoc.skip=true
    - chmod 777 scripts/check.sh
    - scripts/check.sh

cache:
  directories:
  - $HOME/.m2

branches:
  only:
    - master

notifications:
  email:
    recipients:
      - $EMAIL
    on_success: always
    on_failure: always

